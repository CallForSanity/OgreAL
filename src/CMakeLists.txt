ADD_DEFINITIONS(-DOGREAL_EXPORT)

FILE(GLOB_RECURSE src "*.cpp" "*.h")
ADD_LIBRARY(OgreAL SHARED ${src})
TARGET_LINK_LIBRARIES(OgreAL ${OGRE_LIBRARIES} ${OPENAL_LIBRARY} ${OGGVORBIS_LIBRARIES})

INSTALL(TARGETS OgreAL DESTINATION lib)
